/** Host loader entry for the skill-plaza settings section browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the skill plaza settings section. */
export function apply(): void {}
