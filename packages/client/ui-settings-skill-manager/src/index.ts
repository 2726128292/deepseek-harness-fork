/** Host loader entry for the skill-manager settings section browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the skill manager settings section. */
export function apply(): void {}
